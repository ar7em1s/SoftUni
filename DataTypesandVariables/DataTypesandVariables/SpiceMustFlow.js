function dune(startYield) {
    let extraction = 0;
    while (startYield > 100) {
    }
    //starting yied for the 1st day
    // after the 1st day, yield drops by 10 per day 
    //
    //mining crew consumes 26 per day at th eend of their shift 
    //additional 26 at the end

    


}

dune(111); 
console.log("--------");
dune(450); 