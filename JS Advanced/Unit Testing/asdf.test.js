import { expect } from "chai";
import { cookingNumbers } from "./CookingbyNumbers.js";

describe("Test suite", function () {
  it("should load libraries", () => {
    expect(1).to.equal(1);
  });
});
